
export interface InvitationData {
  vidhyalayName: string;
  guestName: string;
  date: string;
  time: string;
  nivedak: string;
  templateImage?: string; // Base64 or URL of the uploaded template
}
